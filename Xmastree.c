#include<stdio.h>
#include<conio.h>

int main()
{
    int a,b,c,n,i;
    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        scanf("%d %d %d ",&a,&b,&c);
        if ((a>=200 && a<=300) && if (b>=50) && if (c>=150))
        printf("Sim\n");
        else
            printf("Nao\n");

    }
    return 0;
}
